////////////// Global Variables ///////////////////////////////////////////////
var projectsArray = [];

// This is where the project constructor will go
//
// It will be very similar to the article constructor used in the pair programming assignment

function project(obj) = {
  this.name = obj.name;
  this.url = obj.url;
  this.title = obj.title;
  this.body = obj.body;
  this.img = obj.img;
  ///etc //// will determine what all to include later
}
